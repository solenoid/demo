{
  "name": "icons-shared",
  "private": true,
  "version": "0.0.0",
  "description": "Shared icon components.",
  "main": "./dist/index.ts",
  "types": "./dist/index.ts",
  "scripts": {
    "src:gen": "svgr --typescript --icon 24 --out-dir dist --jsx-runtime automatic svg-src",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@svgr/cli": "6.5.1",
    "@types/react": "18.0.26"
  }
}
