{
  "name": "shared-icons",
  "private": true,
  "version": "0.0.0",
  "description": "Shared Icon Components.",
  "main": "./dist/index.ts",
  "types": "./dist/index.ts",
  "scripts": {
    "code-gen": "svgr --no-prettier --typescript --icon 24 --out-dir dist --jsx-runtime automatic svg-src",
    "clean": "rimraf node_modules dist"
  },
  "devDependencies": {
    "@svgr/cli": "8.1.0",
    "@types/react": "18.2.39"
  }
}
