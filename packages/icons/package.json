{
  "name": "@solenoid/icons",
  "private": true,
  "version": "0.0.0",
  "description": "Shared Icons across apps.",
  "main": "./dist/index.ts",
  "types": "./dist/index.ts",
  "scripts": {
    "src:gen": "npm exec -y -- @svgr/cli@6.5.1 --typescript --icon 24 --out-dir dist -- svg-src",
    "clean": "npm exec -y -- rimraf@3.0.2 .turbo dist"
  }
}
