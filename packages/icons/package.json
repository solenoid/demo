{
  "name": "@solenoid/icons",
  "private": true,
  "version": "0.0.0",
  "description": "Shared Icons across apps.",
  "main": "./build/index.ts",
  "types": "./build/index.ts",
  "scripts": {
    "build": "npm exec -y -- @svgr/cli@6.5.1 --typescript --icon 24 --out-dir build -- svg-src",
    "clean": "npm exec -y -- rimraf@3.0.2 .turbo build"
  }
}
